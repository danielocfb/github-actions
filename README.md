github-actions
==============

Reusable GitHub Actions workflows used in the `libbpf` org.

We provide no backwards compatibility guarantees short of not clobbering
`git` commit history. Be sure to pin the snapshot you consume.
